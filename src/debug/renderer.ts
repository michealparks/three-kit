import { renderer } from '../lib'
import { addFolder, Panes } from './pane'

export const addRendererFolder = (pane: Panes) => {
  // const folder = addFolder(pane, 'renderer')

  // const params = {

  // }

  // folder.addInput()
}